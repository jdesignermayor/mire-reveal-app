"use client"

import Image from "next/image"
import { useEffect, useState } from "react"

const images = [
  {
    src: "/demos/realistic_1.jpg",
    alt: "Elegant interior design",
  },
  {
    src: "/demos/realistic_2.jpg",
    alt: "Sophisticated workspace",
  },
  {
    src: "/demos/realistic_3.jpg",
    alt: "Peaceful environment",
  },
]

export function ImageCarousel() {
  const [currentIndex, setCurrentIndex] = useState(0)

  useEffect(() => {
    const interval = setInterval(() => {
      setCurrentIndex((prevIndex) => (prevIndex + 1) % images.length)
    }, 6000)

    return () => clearInterval(interval)
  }, [])

  return (
    <div className="relative hidden lg:block overflow-hidden bg-primary">
      {images.map((image, index) => (
        <div
          key={index}
          className={`absolute inset-0 transition-opacity duration-1000 ${
            index === currentIndex ? "opacity-100" : "opacity-0"
          }`}
        >
          <Image
            src={image.src || "/placeholder.svg"}
            alt={image.alt}
            fill
            quality={100}
            className="object-cover"
            priority={index === 0}
          />
        </div>
      ))}

      <div className="absolute inset-0 bg-gradient-to-t from-primary/80 via-primary/40 to-transparent" />

      <div className="absolute bottom-0 left-0 right-0 p-12">
        <h2 className="text-6xl font-bold text-primary-foreground mb-4 text-balance font-poppins">
        Create hyper-realistic ultrasounds in seconds.
        </h2>
        <p className="text-2xl text-primary-foreground/90 max-w-md leading-relaxed">
          Hyper-realistic images generated by AI so you can share and remember every special moment.
        </p>

        <div className="flex gap-2 mt-8">
          {images.map((_, index) => (
            <button
              key={index}
              onClick={() => setCurrentIndex(index)}
              className={`h-1.5 rounded-full transition-all duration-300 ${
                index === currentIndex
                  ? "bg-primary-foreground w-12"
                  : "bg-primary-foreground/40 w-8 hover:bg-primary-foreground/60"
              }`}
              aria-label={`Go to slide ${index + 1}`}
            />
          ))}
        </div>
      </div>
    </div>
  )
}
